{
  "name": "database",
  "version": "1.0.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "rm -rf lib && esbuild `find src \\( -name '*.ts' -o -name '*.tsx' \\)` --platform='node' --sourcemap --ignore-annotations --format='cjs' --target='es2022' --outdir='lib' && esbuild src/index.ts --platform='node' --sourcemap --ignore-annotations --format='cjs' --target='es2022' --outdir='lib' --banner:js='require(\"module-alias\").addAlias(\"@\", __dirname);'",
    "kit": "drizzle-kit",
    "lint": "tsc",
    "dev": "pnpm build && cd lib && NODE_ENV=development node --enable-source-maps index && cd ..",
    "migrate:clickhouse": "pnpm build && cd lib && NODE_ENV=development node --enable-source-maps index migrate-clickhouse && cd ..",
    "migrate:postgres": "drizzle-kit migrate"
  },
  "keywords": [],
  "author": "MCJars",
  "license": "MIT",
  "dependencies": {
    "@clickhouse/client": "^1.18.2",
    "@rjweb/utils": "^1.12.29",
    "ahocon": "^0.2.7",
    "ansi-colors": "^4.1.3",
    "drizzle-orm": "^0.41.0",
    "js-yaml": "^4.1.1",
    "json5": "^2.2.3",
    "module-alias": "^2.3.4",
    "pg": "^8.20.0",
    "toml": "^4.1.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.12.2",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "esbuild": "^0.27.7",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
